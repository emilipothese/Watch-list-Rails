<h1>Add a movie to the list</h1>

<%= simple_form_for(@bookmark) do |f| %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <%= f.label :id_movie %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>
